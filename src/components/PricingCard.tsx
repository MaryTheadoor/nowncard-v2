// PricingCard component - temporarily disabled while establishing clean baseline
// Will be restored with provider-agnostic checkout once baseline is verified
export default function PricingCard() {
  return null;
}
